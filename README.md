# homebrew-tap

Homebrew tap for [xinquiry](https://github.com/xinquiry) projects.

## Usage

```sh
brew tap xinquiry/tap
brew install koda
```

Or in one shot:

```sh
brew install xinquiry/tap/koda
```

Formulas in `Formula/` are updated automatically by each project's release workflow.
